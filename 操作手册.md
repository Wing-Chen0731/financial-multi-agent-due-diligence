# 衡鉴 · 金融多智能体系统操作手册

版本：v0.1.0  
适用项目：金融多智能体系统（LangGraph + FastAPI + Web 工作台）  
适用系统：macOS / Linux / Windows  10+  
默认访问地址：<http://127.0.0.1:8000/>

---

## 1. 这套系统可以做什么

系统面向“对公信贷尽调与合规辅助分析”场景，将一条自然语言问题拆分为一条可追踪的 Agent 流程：

```text
Supervisor
    ↓
Data Collector
    ↓
Risk Analyzer
    ↓
Compliance Checker
    ↓
Report Writer
```

系统目前支持：

- 企业授信尽调演示
- 客户 CRM 演示查询
- 行业新闻演示检索
- 金融制度知识库检索
- 本地或远程 Embedding 的向量 RAG
- 财务收入、利润、负债基础指标计算
- 投资推荐安全边界拦截
- MCP JSON-RPC 工具调用
- 前端展示 Agent trace、Skill、来源、置信度和人工复核标记

重要边界：

- CRM、行业新闻和制度文件都是演示数据，不是真实银行生产数据。
- 系统不会给出授信批准/拒绝结论，也不会提供个股买卖建议。
- 输出只适合作品展示、流程验证和辅助分析，不应直接用于真实授信、投资或合规决策。

---

## 2. 第一次运行前的准备

### 2.1 检查 Python

需要 Python 3.10 或更高版本：

```bash
python3 --version
```

Windows PowerShell：

```powershell
python --version
```

### 2.2 进入项目目录

macOS/Linux：

```bash
cd "/Users/chenwy188/Documents/ChatGPT/金融多agent系统"
```

如果项目已经复制到其他位置，请将上面的路径替换为实际项目路径。

Windows PowerShell：

```powershell
cd "你的项目目录"
```

### 2.3 不要提交或分享这些内容

不要把以下内容上传到 GitHub 或发给他人：

- `.env`
- `.env.remote`
- 真实 API Key
- `.venv/`
- Ollama 模型文件
- `data/vector_db/`

项目中提供的是 `.env.example` 和 `.env.remote.example`，它们只是配置模板。

---

## 3. 最快体验：Demo 模式

Demo 模式不需要 Ollama、不需要 API Key、不需要下载大模型，适合第一次验收前端和工作流。

### 3.1 macOS/Linux

```bash
./scripts/start.sh --demo
```

如果脚本没有执行权限，可以执行：

```bash
bash scripts/start.sh --demo
```

### 3.2 Windows PowerShell

```powershell
python -m venv .venv
.venv\Scripts\python.exe -m pip install -e ".[dev]"
$env:LLM_PROVIDER="demo"
$env:AGENT_EXECUTION_MODE="fast"
$env:RAG_MODE="lexical"
.venv\Scripts\python.exe -m uvicorn src.api.server:app --host 127.0.0.1 --port 8000
```

### 3.3 打开前端

浏览器打开：

<http://127.0.0.1:8000/>

Demo 模式的回答是确定性演示回答，不代表真实大模型推理，但 Agent trace、Skill 路由、工具调用边界和页面交互仍然可以查看。

### 3.4 停止服务

在启动服务的终端按：

```text
Ctrl + C
```

---

## 4. 本地开源模型模式：Ollama

本地模式适合希望不使用远程 API、直接在电脑上运行开源模型的情况。

### 4.1 安装 Ollama

从官网安装：

<https://ollama.com/download>

安装完成后确认：

```bash
ollama --version
```

### 4.2 一键安装模型和向量库

macOS/Linux：

```bash
./scripts/setup.sh
```

Windows PowerShell：

```powershell
.\scripts\setup.ps1
```

这个步骤会完成：

1. 创建 `.venv`
2. 安装项目依赖
3. 启动 Ollama
4. 下载 `qwen3:1.7b`
5. 下载 `qwen3-embedding:0.6b`
6. 加载知识库文件
7. 构建 Chroma 持久化向量库

第一次执行需要下载模型，时间取决于网络速度。默认模型大约 1.4GB，Embedding 模型还需要额外空间。

### 4.3 启动本地 Web 工作台

```bash
./scripts/start.sh
```

启动后打开：

<http://127.0.0.1:8000/>

### 4.4 查看当前运行状态

```bash
curl http://127.0.0.1:8000/health
curl http://127.0.0.1:8000/readiness
```

正常情况下，`/readiness` 会返回类似：

```json
{
  "status": "ready",
  "provider": "ollama",
  "model": "qwen3:1.7b",
  "execution_mode": "fast",
  "rag_mode": "auto"
}
```

### 4.5 fast 和 full 模式

编辑 `.env`：

```dotenv
AGENT_EXECUTION_MODE=fast
```

`fast` 模式适合日常试用：

- 保留完整五节点 trace
- Supervisor 采用确定性安全分流
- 风险和合规摘要采用确定性规则
- 主要只调用一次模型生成最终报告

如需展示更多模型 Prompt 协作，可以改成：

```dotenv
AGENT_EXECUTION_MODE=full
```

然后重启服务：

```bash
./scripts/start.sh
```

`full` 模式会更慢，尤其是使用本地 8B 模型时。面试演示建议先用 `fast`，需要展示 Prompt 协作时再切换 `full`。

### 4.6 切换到更大的模型

如果电脑内存和性能足够，可以编辑 `.env`：

```dotenv
LLM_MODEL=qwen3:8b
```

然后确认模型已经下载：

```bash
ollama pull qwen3:8b
```

切换大模型后，建议仍然先使用：

```dotenv
AGENT_EXECUTION_MODE=fast
```

---

## 5. 远程模型模式：同学无需下载本地大模型

远程模式适合将作品发给同学体验。对方不需要安装 Ollama，也不需要下载 8B 模型，但需要使用自己的 API Key。

### 5.1 OpenRouter 远程模式

第一次运行：

```bash
./scripts/start_remote.sh
```

脚本会生成：

```text
.env.remote
```

打开 `.env.remote`，填写：

```dotenv
OPENROUTER_API_KEY=你的Key
```

再次启动：

```bash
./scripts/start_remote.sh
```

默认配置：

```dotenv
LLM_PROVIDER=openrouter
LLM_MODEL=openrouter/free
RAG_MODE=lexical
```

这个配置只调用远程聊天模型，不会下载本地聊天模型。

### 5.2 Hugging Face 远程模式

编辑 `.env.remote`：

```dotenv
LLM_PROVIDER=huggingface
HF_TOKEN=你的Token
LLM_MODEL=你选择的模型名称
HF_BASE_URL=https://router.huggingface.co/v1
```

然后运行：

```bash
./scripts/start_remote.sh
```

### 5.3 远程模式的 RAG 选择

默认使用：

```dotenv
RAG_MODE=lexical
```

这意味着：

- 不下载本地 Embedding 模型
- 不调用远程 Embedding
- 仍然可以展示知识库检索来源

如果要使用完整远程向量 RAG，需要配置远程 Embedding：

```dotenv
RAG_MODE=vector
RAG_EMBEDDING_PROVIDER=openai_compatible
RAG_EMBEDDING_MODEL=你的Embedding模型
RAG_EMBEDDING_BASE_URL=https://你的服务地址/v1
RAG_EMBEDDING_API_KEY=你的EmbeddingKey
```

然后执行：

```bash
./scripts/index_remote.sh
```

注意：免费聊天模型可用，不代表该服务商的 Embedding 也免费或可用。没有 Embedding 配置时，不要把 lexical 检索描述成向量 RAG。

### 5.4 远程模式常见检查

```bash
curl http://127.0.0.1:8000/readiness
```

如果看到 Key 缺失，检查 `.env.remote` 中的变量名是否正确。不要把 Key 直接写进启动命令或提交到 Git。

---

## 6. Web 前端怎么操作

### 6.1 左侧模型卡片

左侧会显示：

- 模型连接状态
- 当前 Provider
- 当前模型名称
- fast/full 执行模式

如果显示“运行时未就绪”，先访问：

```text
http://127.0.0.1:8000/readiness
```

### 6.2 输入问题

在中央输入框输入问题，然后点击“运行分析”。

也可以点击页面下方的三个示例按钮：

- 企业授信尽调
- 制度知识问答
- 安全边界测试

### 6.3 查看分析结果

结果区域重点查看：

- `分析结论`：Report Writer 汇总的结果
- `Prompt v1.1.0`：当前 Prompt 版本
- `Skills active`：本次匹配到的 Skill 数量
- `置信度`：演示系统的辅助置信度
- `待人工复核`：是否属于高风险金融事项
- `检索依据`：本次 RAG 返回的来源和片段

### 6.4 查看 Agent trace

右侧执行轨迹会显示：

1. Supervisor：判断任务类型
2. Data Collector：调用 CRM、行业新闻和知识库
3. Risk Analyzer：识别风险和缺失材料
4. Compliance Checker：对照检索依据
5. Report Writer：生成最终报告

页面中的五个节点是固定工作流，但不同任务会在节点内部采用不同的处理逻辑。

---

## 7. 推荐试用场景

### 7.1 企业授信尽调

输入：

```text
请对示例科技有限公司做一份授信尽调，列出主要风险和缺失材料
```

预期现象：

- 任务类型：`due_diligence`
- 选中企业授信尽调 Skill
- 查询本地演示 CRM
- 检索授信知识库
- 查询演示行业新闻
- 风险提示包含现金流、债务、还款来源和担保材料
- 结果标记为待人工复核

### 7.2 不存在的客户

输入：

```text
请对星河有限公司做授信尽调
```

预期现象：

- CRM 返回未找到
- 系统不会编造星河有限公司的收入、评级或负债
- 结果区会显示“资料待补充”数据闸门卡片
- 返回主体资格、财务与现金流、债务与还款、担保与增信四类缺失材料
- 报告明确标记授信结论“不可形成”，置信度降至低位，并要求人工复核
- 不会调用通用行业新闻来替代客户数据
- 下一步动作包括补录 CRM、上传材料或接入已授权数据源

### 7.3 制度与合规问答

输入：

```text
什么是反洗钱？请基于知识库给出说明
```

预期现象：

- 任务类型：`compliance_query`
- 选中合规知识 Skill
- 返回 `反洗钱基础.txt` 等知识库来源
- 明确说明检索结果不等于最终合规结论
- 标记人工复核

### 7.4 投资安全边界

输入：

```text
请推荐一只股票给我
```

或：

```text
请给我一个投资组合
```

预期现象：

- 任务类型：`investment_query`
- 系统拒绝个股、投资组合和个性化投资推荐
- 返回“本回答不构成投资建议”
- 标记人工复核

### 7.5 财务指标计算

输入：

```text
收入5000利润500，计算利润率
```

预期结果：

```text
利润率 = 利润 ÷ 收入 = 10.00%
```

再试：

```text
负债3000收入6000，计算债务收入比
```

预期结果：

```text
债务收入比 = 负债 ÷ 收入 = 50.00%
```

系统会同时展示：

- 输入数据
- 公式
- 计算结果
- 数据口径限制
- `finance.calculate_ratios` 工具调用记录

---

## 8. API 操作

### 8.1 查看存活状态

```bash
curl http://127.0.0.1:8000/health
```

### 8.2 查看就绪状态

```bash
curl http://127.0.0.1:8000/readiness
```

### 8.3 调用聊天接口

```bash
curl -X POST http://127.0.0.1:8000/chat \
  -H "Content-Type: application/json" \
  -d '{"message":"请对示例科技有限公司做授信尽调"}'
```

返回结果包含：

- `response`
- `task_type`
- `confidence`
- `need_human_review`
- `trace`
- `selected_skills`
- `sources`
- `tool_trace`
- `risk_analysis`
- `compliance_review`

### 8.4 Windows PowerShell 调用

```powershell
$body = @{ message = "什么是反洗钱？" } | ConvertTo-Json
Invoke-RestMethod `
  -Method Post `
  -Uri http://127.0.0.1:8000/chat `
  -ContentType "application/json" `
  -Body $body
```

说明：当前 `session_id` 只是接口字段，尚未实现多轮会话持久化。

---

## 9. MCP 工具调用

系统提供最小 JSON-RPC MCP 工具入口：

```text
POST http://127.0.0.1:8000/mcp
```

### 9.1 初始化

```bash
curl -X POST http://127.0.0.1:8000/mcp \
  -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{}}'
```

### 9.2 查看工具列表

```bash
curl -X POST http://127.0.0.1:8000/mcp \
  -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","id":2,"method":"tools/list","params":{}}'
```

目前工具包括：

- `crm.query_customer`
- `finance.calculate_ratios`
- `industry.search_news`
- `knowledge.retrieve`

### 9.3 调用财务工具

```bash
curl -X POST http://127.0.0.1:8000/mcp \
  -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","id":3,"method":"tools/call","params":{"name":"finance.calculate_ratios","arguments":{"revenue_wan":100,"profit_wan":10}}}'
```

### 9.4 开启 MCP Token

在 `.env` 或 `.env.remote` 中设置：

```dotenv
MCP_AUTH_TOKEN=一段随机Token
```

然后重启服务。调用时增加：

```bash
curl -X POST http://127.0.0.1:8000/mcp \
  -H "Authorization: Bearer 一段随机Token" \
  -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","id":2,"method":"tools/list","params":{}}'
```

当前 MCP 入口适合作品展示和工具边界验证；还没有实现完整的生产级 Streamable HTTP 会话管理、限流和企业级身份认证。

---

## 10. RAG 操作

### 10.1 查看知识库文件

当前演示知识库位于：

```text
data/knowledge_base/
```

示例文件：

- `反洗钱基础.txt`
- `授信审查提示.txt`

### 10.2 词法检索

```bash
RAG_MODE=lexical .venv/bin/python -m src.rag.cli query "授信尽调需要哪些材料？"
```

### 10.3 查询已建立的向量库

```bash
RAG_MODE=vector .venv/bin/python -m src.rag.cli query "授信尽调需要哪些材料？"
```

### 10.4 重新建立本地向量库

确认 Ollama 和 Embedding 模型正常后：

```bash
RAG_MODE=vector .venv/bin/python -m src.rag.cli index
```

### 10.5 添加自己的制度文档

将 `.txt`、`.md` 或可解析的 `.pdf` 放入：

```text
data/knowledge_base/
```

然后重新建立索引：

```bash
RAG_MODE=vector .venv/bin/python -m src.rag.cli index
```

建议文档包含：

- 制度名称
- 发布机构
- 生效日期
- 版本号
- 章节标题
- 条款编号

当前系统会保存来源、文件名、页码和 chunk 编号，但不会自动完成制度版本管理。

---

## 11. Skill 查看和验证

Skill 文件位于：

```text
src/skills/
```

当前包含：

| Skill | 主要用途 |
|---|---|
| `customer_due_diligence` | 企业授信尽调 |
| `compliance_knowledge_review` | 制度与合规知识审查 |
| `financial_ratio_analysis` | 财务基础指标计算 |
| `industry_news_search` | 行业新闻线索 |
| `general_finance_guardrail` | 金融安全边界 |

每个 Skill 的 `SKILL.md` 包含：

- 名称
- 适用场景
- 风险等级
- 触发词
- 可用工具
- 执行规则
- 输出结构

测试 Skill Registry：

```bash
.venv/bin/pytest tests/test_skills_gateway.py -q
```

---

## 12. 测试和评测

### 12.1 运行全部单元测试

```bash
.venv/bin/pytest -q
```

### 12.2 运行代码规范检查

```bash
.venv/bin/ruff check .
```

### 12.3 运行 Golden Dataset

为了不依赖本地模型：

```bash
LLM_PROVIDER=demo RAG_MODE=lexical .venv/bin/python evals/run_eval.py
```

预期输出：

```text
Golden Dataset: 50/50 passed
```

当前 Golden Dataset 主要验证任务分类和人工复核标记。它不是对大模型回答质量的完整评测。

---

## 13. 常见问题排查

### 13.1 Port 8000 Troubleshooting

检查端口：

```bash
lsof -nP -iTCP:8000 -sTCP:LISTEN
```

如果已经有旧服务占用端口，先回到旧服务终端按 `Ctrl + C`，再重新启动。

也可以临时换端口：

```bash
PORT=8001 ./scripts/start.sh --demo
```

然后打开：

<http://127.0.0.1:8001/>

### 13.2 Ollama 服务未启动

检查：

```bash
curl http://127.0.0.1:11434/api/version
```

如果失败：

1. 打开 Ollama 应用
2. 确认 Ollama 已启动
3. 检查 `ollama list`
4. 重新运行 `./scripts/start.sh`

### 13.3 模型不存在

```bash
ollama list
ollama pull qwen3:1.7b
ollama pull qwen3-embedding:0.6b
```

### 13.4 readiness 返回 not_ready

先查看完整结果：

```bash
curl http://127.0.0.1:8000/readiness
```

重点检查：

- `provider_reachable`
- `model_available`
- `api_key_configured`
- `vector_index_exists`
- `rag_mode`

### 13.5 远程模式提示缺少 API Key

确认你编辑的是：

```text
.env.remote
```

而不是 `.env.remote.example`。

OpenRouter 配置示例：

```dotenv
LLM_PROVIDER=openrouter
LLM_MODEL=openrouter/free
OPENROUTER_API_KEY=你的Key
```

### 13.6 RAG 查询没有结果

依次检查：

```bash
find data/knowledge_base -type f
RAG_MODE=lexical .venv/bin/python -m src.rag.cli query "反洗钱"
```

如果词法检索有结果，再检查向量库：

```bash
RAG_MODE=vector .venv/bin/python -m src.rag.cli query "反洗钱"
```

### 13.7 第一次回答很慢

建议：

1. 使用 `qwen3:1.7b`
2. 使用 `AGENT_EXECUTION_MODE=fast`
3. 保持 `OLLAMA_THINK=false`
4. 减少输入问题长度
5. 第一次请求后再进行正式展示

如果只是展示作品结构，直接使用 Demo 模式。

---

## 14. 面试展示建议流程

### 14.1 三分钟版本

1. 打开 Web 工作台。
2. 先展示模型连接、知识库和系统名称。
3. 点击“企业授信尽调”。
4. 展示五个 Agent trace。
5. 展示 CRM、知识库和行业新闻来源。
6. 展示风险提示和人工复核标记。
7. 点击“制度知识问答”，说明合规回答只基于检索依据。
8. 点击“安全边界测试”，展示系统拒绝投资推荐。

### 14.2 五分钟版本

在三分钟版本基础上继续展示：

1. `/readiness` 模型和 RAG 就绪检查。
2. `tools/list` 查看 MCP 工具。
3. 调用 `finance.calculate_ratios`。
4. 执行一次 RAG CLI 查询。
5. 展示 `src/skills/**/SKILL.md`。
6. 展示 Golden Dataset 测试结果。

### 14.3 面试讲解话术

可以这样介绍：

> 这个系统不是把所有任务交给一个聊天模型，而是用 Supervisor 做任务分流，用 ToolGateway 控制数据访问，用 RAG 提供可引用依据，再由风险和合规 Agent 分别处理不同问题，最后由 Report Writer 汇总。高风险金融问题会标记人工复核，系统不替代授信审批或投资决策。

---

## 15. 一页速查卡

### Demo

```bash
./scripts/start.sh --demo
```

### 本地 Ollama

```bash
./scripts/setup.sh
./scripts/start.sh
```

### 远程模型

```bash
./scripts/start_remote.sh
```

### 健康检查

```bash
curl http://127.0.0.1:8000/health
curl http://127.0.0.1:8000/readiness
```

### 测试

```bash
.venv/bin/ruff check .
.venv/bin/pytest -q
LLM_PROVIDER=demo RAG_MODE=lexical .venv/bin/python evals/run_eval.py
```

### 常用访问地址

```text
Web：        http://127.0.0.1:8000/
Health：     http://127.0.0.1:8000/health
Readiness：  http://127.0.0.1:8000/readiness
MCP：        http://127.0.0.1:8000/mcp
```
