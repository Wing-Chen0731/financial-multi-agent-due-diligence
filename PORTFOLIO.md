# 面试展示说明

## 作品定位

“衡鉴”不是一个泛化聊天机器人，而是一个面向金融场景的决策辅助工作台。它用 LangGraph 把一个金融问题拆成可追溯的协作链：任务主管负责分流，采集 Agent 负责获取事实和检索依据，风险 Agent 负责找问题，合规 Agent 负责对照证据，报告 Agent 负责汇总并加上人工复核护栏。

## 3 分钟演示脚本

1. 打开 `/`，先展示左侧的模型连接、知识库和工作区信息。
2. 点击“企业授信尽调”，解释同一个输入如何进入五个节点。
3. 指向右侧运行视图，说明每一步都会写入 trace，而不是一次性黑盒回答。
4. 指向“检索依据”，说明制度类问题只引用本地知识库，资料不足时明确说没有证据。
5. 点击“安全边界测试”，展示系统不提供个股推荐，并自动标记人工复核。
6. 展示左侧动态运行时：默认是轻量 `qwen3:1.7b + fast`，也可以切换 `AGENT_EXECUTION_MODE=full` 展示完整模型协作。

## 已补齐的作品化内容

- 一个可直接打开的交互前端，而不是只展示 API 或命令行。
- 1 个 Supervisor + 1 个 ToolGateway + 4 个垂直 Agent 的协作链路。
- 5 个可加载的业务 Skill Markdown 规范和 Skill Registry。
- 文档切片、Embedding、Chroma 持久化、混合检索和显式 fallback 的 RAG 链路。
- 50 条模拟 Golden Dataset 回归用例，覆盖正常、边界和红线场景。
- 面向面试的产品叙事：能力地图、架构链路、运行观测和安全护栏。
- 真实可演示的三类场景：企业尽调、制度问答、安全边界测试。
- API 返回 trace、来源、置信度和人工复核状态，前端可解释地呈现结果。
- 默认本地 Ollama 模式不需要 API Key；没有 Ollama 时可用 `./scripts/start.sh --demo` 秒级展示作品。
- 提供 macOS/Linux `setup.sh`、`start.sh` 和 Windows PowerShell 脚本，负责依赖、模型、向量库和 Web 服务启动。
- 提供 `start_remote.sh` / `start_remote.ps1`：同学无需下载本地大模型，填入自己的 OpenRouter 或 Hugging Face Key 即可体验远程开源模型；同时保留 `demo` 和 Ollama 两条兜底路径。
- 远程 RAG 已提供 OpenAI-compatible Embedding 适配器；默认远程分享档位使用 lexical RAG，配置远程 Embedding 后可通过 `index_remote` 构建完整持久化向量库，不混淆两者能力边界。
- ToolGateway 已暴露 MCP JSON-RPC 的 `initialize`、`tools/list` 和 `tools/call` 入口，并支持可选 Bearer Token；健康检查区分进程存活与模型/RAG 就绪状态。
- 财务指标 Skill 已接入主 Graph，用户明确提供收入、利润或负债数字时会调用白名单财务工具并返回公式、结果和口径限制。
- 快速模式保留 5 个节点的可审计 trace，但日常请求只做 1 次模型生成；本机实测约 23 秒，完整模式用于高质量面试展示。

## 仍然需要向面试官主动说明的生产差距

这些不是 Demo 缺陷，而是从作品原型走向生产系统时的下一阶段：

- 知识库现在是演示材料，正式版本要接入经过版本管理的制度文档，并记录生效日期和来源。
- CRM 目前是本地演示工具，生产版本要增加 OAuth / RBAC、字段级脱敏和审计日志。
- 当前已经是 Ollama Embedding + Chroma 持久化向量检索，并在 `auto` 模式融合词法结果；生产规模化时还需要增量索引、rerank、权限过滤和引用准确率评测。
- 还需要增加 LangSmith 或 OpenTelemetry 追踪、离线评测集、幻觉/拒答率/引用准确率指标。
- 需要持久化会话、任务重试、超时与熔断，以及 Docker / CI/CD / 云端部署配置。
- 当前“人工复核”是状态标记，生产版本要接入工单或审批系统形成闭环。
- 远程免费模型依赖供应商额度和可用性，正式部署仍应配置企业级模型网关、限流、成本预算和故障转移策略。

## 面试时可以这样总结

“我刻意没有把金融 Agent 做成只追求回答流畅的聊天框，而是把事实、证据、风险和决策权限拆开。LangGraph 负责状态和流程，工具负责边界内的数据获取，前端把执行轨迹和引用呈现出来。这样系统即使不能做最终审批，也能成为可复核、可迭代的分析基础设施。”
